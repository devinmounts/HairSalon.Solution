@{
    Layout = "_Layout";
}
<div class="container" id="stylist-container">
    <h1>@Model.GetName()</h1>
    <h4>@Model.GetDetails()</h4>
    <a class="btn btn-danger" href="/stylist/all/@Model.GetId()/update">Update Stylist</a>
    <div class="row titles clients">
        <div class="col-md-5">
            <h4>Clients</h4>
        </div>
    </div>
    <div class="row data clients">
        @foreach (var client in Model.FindClients())
        {
        <div class="col-md-5">
            <p class="name">@client.GetName()</p>
        </div>
        }
    </div>
    <div class="row titles specialties">
        <div class="col-md-5">
            <h4>Specialties</h4>
        </div>
    </div>
    <div class="row data specialties">
        @foreach (var specialty in Model.GetSpecialties())
        {
        <div class="col-md-5">
            <p class="name">@specialty.Description</p>
        </div>
        }
    </div>
</div>


